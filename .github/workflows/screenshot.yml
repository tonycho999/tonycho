name: Auto Screenshot Bot

on:
  push:
    paths:
      - 'sites.json'  # sites.json이 수정될 때 자동 실행
  workflow_dispatch:  # 수동으로 버튼 눌러 실행 가능

jobs:
  capture:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install puppeteer

    - name: Run Screenshot Bot
      run: node screenshot-bot.js

    - name: Commit and Push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action Bot"
        git add img/
        git commit -m "🎨 Update screenshots [skip ci]" || echo "No changes to commit"
        git push
