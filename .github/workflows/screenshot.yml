name: Auto Screenshot Bot

on:
  # push 설정을 지웠으므로 파일을 올려도 자동 실행되지 않습니다.
  workflow_dispatch:  # 오직 이 버튼을 눌렀을 때만 실행됩니다.

jobs:
  capture:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install puppeteer

    - name: Run Screenshot Bot
      env:
        ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL }}
        ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
      run: node screenshot-bot.js

    - name: Commit and Push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action Bot"
        git add img/
        git commit -m "🎨 Manual screenshot update [skip ci]" || echo "No changes to commit"
        git push
