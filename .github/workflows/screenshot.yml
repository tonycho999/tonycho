name: Auto Screenshot Bot

on:
  workflow_dispatch: # 버튼 눌러서 수동 실행

jobs:
  capture:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install Dependencies & Chrome
      run: |
        # 1. 패키지 설치
        npm install
        
        # 2. Chrome 브라우저 설치 (시스템 의존성 포함)
        # 이 명령어가 우분투 버전에 맞는 라이브러리를 알아서 찾아 설치해줍니다.
        npx puppeteer browsers install chrome
        
    - name: Run Screenshot Bot
      env:
        ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL }}
        ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
      run: node screenshot-bot.js

    - name: Commit and Push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action Bot"
        git add img/
        # 변경사항이 있을 때만 커밋
        git commit -m "🎨 Update screenshots [skip ci]" || echo "No changes to commit"
        git push
